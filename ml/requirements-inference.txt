# Python version should match the environment used to train (e.g. 3.11+).
# Pin scikit-learn to the same minor version as training when possible.
fastapi>=0.100
uvicorn[standard]>=0.22
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
joblib>=1.3
pydantic>=2.0
